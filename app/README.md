# PhotoSortX デスクトップ（Tauri + React）

ルートの [README.md](../README.md) にプロジェクト全体の説明があります。

## 開発

```bash
npm install
npm run tauri dev
```

API サーバー（`photosortx-api` または `uvicorn src.api_server:app --host 127.0.0.1 --port 8765`）を先に起動してから UI を開いてください。
