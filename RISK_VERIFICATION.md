# 本番データ利用前のリスク検証ガイド

本番の写真フォルダで運用する前に、次の項目を確認することを推奨します。

## 1. データ消失まわり（何がファイルを消すか）

| 操作 | ディスク上の写真への影響 |
|------|--------------------------|
| スキャン（フォルダ同期） | **削除しない**。DB の登録の追加・整理のみ。 |
| DB 登録パスの実在確認（スキャン開始時） | **削除しない**。実在しないパスの **DB 行だけ** 削除。 |
| 各画面の「削除」→ ゴミ箱へ移動 | 指定フォルダ内から **削除フォルダへ移動**（`shutil.move`）。 |
| 削除済み一覧の「完全に削除」 | **ファイルを削除**（`os.remove`）。退避期間経過後のみ可（既定）。 |
| DB 全初期化 | **画像ファイルは触らない**。`photos.db` 等のみ削除。 |

**検証のすすめ:** 最初は **写真のコピー先フォルダ** と **別名の DB**（`photos.db` をコピーしない／空の DB で開始）で 1 周操作する。

## 2. ゴミ箱の退避期間（完全削除の遅延）

- `src/config.py` の **`TRASH_RETENTION_DAYS`**（既定 14 日）経過までは、「完全に削除」が **DB 層と UI の両方で拒否**されます。
- **`ENFORCE_TRASH_RETENTION_BEFORE_PERMANENT_DELETE`** を `False` にすると従来どおり即時完全削除可能（緊急時のみ推奨）。
- **`TRASH_RETENTION_DAYS`** を `0` にすると、強制有効時でも期間チェックは実質すぐ通過します。

既存 DB でゴミ箱に入っていたが **`trashed_at` が無い行**は後方互換のため、完全削除は **許可**されます（新規にゴミ箱へ入れた行から記録されます）。

## 3. ディスク負荷・発熱

- 大量ファイルのスキャン・解析は I/O と CPU を使います。
- `src/config.py` の **`LOW_LOAD_MODE = True`** で、スキャン・実在確認ループに短いスリープを挟めます（所要時間は伸びます）。
- 一度に扱う枚数を増やす前に、**枚数を小さくしたコピー**で時間感を測ると安全です。

## 4. NAS・ネットワークドライブ

- パスが一時的に見えないと、**「存在しない」として DB 行が削除される**可能性があります。
- 本番をネットワークパスにする場合は、**マウント安定後に同期する**運用を推奨します。

## 5. 推奨チェックリスト（実行前）

- [ ] テスト用コピーフォルダでスキャン→解析→ゴミ箱移動→復元→（期間後）完全削除まで通した
- [ ] 本番で使う `photos.db` を誤って消さない（バックアップ方針を決めた）
- [ ] 削除フォルダの保存先を確認した（設定画面・エクスプローラで実在を確認）
- [ ] `TRASH_RETENTION_DAYS` が運用に合う日数か確認した
- [ ] （任意）`debug.log` でエラーが出ていないか確認した

## 6. 不具合・想定外時

- ログ: プロジェクト直下の **`debug.log`**
- 同期や DB がおかしいとき: まず **本番フォルダを触らず**、コピー環境で再現を取ると安全です。
