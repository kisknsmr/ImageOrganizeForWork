# PhotoSortX - プログラム完成度評価（2026-09-13 更新）

## 概要

| 項目 | 値 |
|------|----|
| Python ファイル数 | 26（PyQt6版撤去前は46） |
| Python 総コード行数 | 約 5,900 行（撤去前は約 14,100 行） |
| フロントエンド（TS/TSX）ファイル数 | 44 |
| フロントエンド総コード行数 | 約 5,600 行 |
| バックエンドテストケース数 | 161（全通過） |
| パッケージ構成 | `src/`（バックエンドコア + `services/`）、`app/`（Tauri + React UI）、`tests/` |

---

## A. 完成度が高い点

| カテゴリ | 内容 |
|----------|------|
| **UI移行** | PyQt6モノリス → Tauri + React + FastAPI への移行が完了。旧UI（`main.py`/`gui/`/`modules/`）は2026-09-13に撤去済み |
| **機能の網羅性** | フォルダ同期・解析・前処理（振り分け／空フォルダ削除）・重複/ピンボケ/類似整理・手動仕分け・トリアージ・スマート整理（イベントグルーピング）・削除済み一覧まで一通り実装されている |
| **バックエンド構成** | `src/api_server.py`（FastAPI）+ `src/services/`（スキャン・解析・重複・整理等のサービス層）に整理。PyQt6依存が完全に排除され、`api_server.py` は単体でimport・起動可能 |
| **差分更新** | `path_under_root` による誤削除防止、別フォルダ選択時の自動置き換え |
| **デスクトップ統合** | Tauri（`app/src-tauri/`）が起動時に `.venv` の uvicorn を自動起動・終了時に停止。二重起動防止あり |
| **テスト** | バックエンド161件全通過。フロントエンド `tsc --noEmit` エラーなし |
| **依存管理** | `requirements.txt` / `pyproject.toml` からPyQt6を除去済み。`ai`（torch/transformers）はオプション依存として分離 |

---

## B. 不足点・既知のギャップ

### B-1. 機能ギャップ

| # | 深刻度 | 内容 |
|---|--------|------|
| 1 | **高** | **CLIPベースの内容分類が新UIで使えない** — 旧 `modules/ai_classifier.py`（QThread依存）を撤去したため、`src/services/organize_service.py` の `capabilities()` は `content`/`hybrid` を常に `False` で返す。使えるのは時間ベースのグルーピングのみ。Qt非依存の形で再実装が必要（[開発ロードマップ.md](開発ロードマップ.md) セクション4参照） |
| 2 | 中 | **配布パッケージング未着手** — Tauriアプリは開発機の `.venv` を直接参照して起動する方式で、Pythonバックエンドを同梱したインストーラー（Nuitka化・サイドカー同梱）は未実装。リポジトリを移動すると動かなくなる |
| 3 | 低 | **トリアージモードが仕様の一部のみ実装** — Undo・キーボード操作・ドラッグ移動は実装済みだが、進捗バー表示・次3枚の先読み・スワイプ操作は未実装 |

### B-2. コード品質

| # | 優先度 | 内容 |
|---|--------|------|
| 4 | 低 | フロントエンド `eslint.config.js` に `app/src-tauri/target/` の除外設定がなく、ビルド生成物を誤って走査してエラーになる（実コードの問題ではない） |
| 5 | 低 | `FolderDropPanel.tsx` の `useMemo` 依存配列に `isExpanded` が抜けている（react-hooks/exhaustive-deps 警告） |

### B-3. ドキュメント

| # | 優先度 | 内容 |
|---|--------|------|
| 6 | 低 | `pyproject.toml` の `version = "2.3.0"` がREADME/CHANGELOGの「v3.0」表記と不一致 |

### B-4. テスト

| # | 優先度 | 内容 |
|---|--------|------|
| 7 | 中 | フロントエンドに自動テストがない（`tsc`/`eslint` の静的チェックのみ） |
| 8 | 低 | GUI操作のE2Eテストがない（起動→Scan→Analyze→各画面遷移の自動検証なし） |

### B-5. リポジトリ管理

| # | 優先度 | 内容 |
|---|--------|------|
| 9 | 低 | CI（GitHub Actions等）が未設定。テスト・型チェック・lintが手動実行のみ |

---

## C. 総評

### スコア（5段階）

| 項目 | 評価 | 備考 |
|------|------|------|
| 機能完成度 | ★★★★☆ | 新UIが旧PyQt6版の全機能を代替し、トリアージ機能も追加済み。CLIP内容分類のみ未移植 |
| コード品質 | ★★★★☆ | バックエンドは旧UI依存を完全排除しテストも厚い。フロントエンドは型・lintともクリーン |
| テスト | ★★★☆☆ | バックエンドは161件で手厚いが、フロントエンドに自動テストがない |
| ドキュメント | ★★★★☆ | README/CHANGELOGは現状に追従済み。バージョン表記の細かい不一致のみ残る |
| 配布準備 | ★★☆☆☆ | 開発環境前提の起動方式のまま。インストーラー配布には追加作業が必要 |

### 一言

**旧PyQt6版からTauri版への移行は実質完了。** 機能面では新UIが旧版を上回り（トリアージ追加）、コードベースも大幅に整理された（Python側は約14,100行→約5,900行）。残る主な課題は「CLIPベースのAI分類の再実装」と「配布パッケージング」の2点で、いずれも [開発ロードマップ.md](開発ロードマップ.md) に記載済み。
