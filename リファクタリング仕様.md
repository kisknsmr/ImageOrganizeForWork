# PhotoSortX AI Edition — リファクタリング仕様書

> **対象**: バイブコーディングエージェント（Cursor, Claude Code, Copilot等）
> **作成日**: 2026-04-25
> **バージョン**: v2.3 → v3.0 移行用

---

## 1. プロジェクト概要

### 1.1 プロダクトの目的

スマホに溜まった**数十万枚の画像を爆速で取捨選択する**デスクトップアプリ。
AI（CLIP）と従来の画像処理技術（ハッシュ、ぼけ検出、類似度）を組み合わせた本格的なフォトライブラリ管理ツール。販売レベルの品質を目指す。

### 1.2 現状（v2.3）の技術スタック

| 領域 | 技術 |
|------|------|
| GUI | PyQt6 6.10（モノリス構成） |
| 画像処理 | OpenCV, Pillow 12.1, imagehash |
| AI | PyTorch + transformers（CLIP） |
| DB | SQLite（WALモード、RLockによるスレッドセーフ） |
| クラスタリング | scikit-learn（DBSCAN） |
| 類似検索 | VP-Tree + pHash |

### 1.3 現状のディレクトリ構成

```
PhotoSortX/
├── main.py                    # メインウィンドウ・サイドバー・ページ切り替え
├── src/
│   ├── config.py              # AppConfig dataclass
│   ├── database.py            # DatabaseManager（RLock + SQLite）
│   ├── core.py                # ScannerThread, AnalyzerThread, ハッシュ・ぼけ計算
│   └── utils.py               # パス検証・ファイルサイズ整形等
├── gui/
│   ├── gallery_page.py        # 3ペインギャラリー
│   └── workers.py             # 起動時遅延ロード、DB再構築
└── modules/
    ├── duplicate_ui.py        # MD5重複検出・削除UI
    ├── blur_ui.py             # Laplacian varianceぼけ検出UI
    ├── similarity_ui.py       # VP-Tree + pHash類似グループUI
    ├── sorter_ui.py           # CLIPスマート整理UI
    ├── ai_classifier.py       # CLIPモデル管理（AIWorker）
    ├── event_grouper.py       # DBSCAN イベントクラスタリング
    ├── manual_sorter_ui.py    # 2ペイン手動仕分けUI
    └── trash_list_ui.py       # ゴミ箱管理（14日保持）
```

### 1.4 現状のDBスキーマ

```sql
files         -- id, path, hash_value, p_hash, blur_score, full_hash, status, trashed_at
thumbnails    -- file_id, data (JPEG BLOB)
settings      -- key/value (root_path, trash_folder等)
custom_categories -- ユーザー定義カテゴリ
```

---

## 2. アーキテクチャ移行：PyQt6 モノリス → Tauri + Python ハイブリッド

### 2.1 移行の動機

- PyQt6ではCSSアニメーション、スムーズなスクロール、スワイプ操作など**販売品質のUIが実現困難**
- Python側のコアロジック（画像処理、AI、DB）は十分に成熟しており、**書き直す必要がない**
- UIだけをWeb技術（React）で新規構築し、表現力とUXを大幅に向上させる

### 2.2 新アーキテクチャ

```
┌─────────────────────┐          ┌─────────────────────┐
│   フロントエンド      │          │   バックエンド        │
│   (Tauri + React)    │◄── API ──►│   (Python)           │
│                      │  JSON /   │                      │
│ - ギャラリーUI       │ WebSocket │ - core.py（変更なし） │
│ - トリアージUI       │          │ - database.py（変更なし）│
│ - 各クリーンアップ画面│          │ - ai_classifier.py   │
│ - アニメーション     │          │   （変更なし）        │
│ - Tailwind CSS       │          │ - api_server.py（新規）│
└─────────────────────┘          └─────────────────────┘
```

### 2.3 新ディレクトリ構成

```
photosortx/
├── core/                       # ← 既存Python資産（90%再利用）
│   ├── core.py                 # スキャン・解析（変更なし）
│   ├── database.py             # DB操作（変更なし）
│   ├── ai_classifier.py        # CLIP（変更なし）
│   ├── config.py               # 設定（変更なし）
│   ├── utils.py                # ユーティリティ（変更なし）
│   └── api_server.py           # 【新規】FastAPI ラッパー
│
├── app/                        # ← 新規：Tauri + React
│   ├── src-tauri/              # Tauri設定 + Pythonサイドカー起動
│   └── src/                    # React UI
│       ├── pages/              # Home, Gallery, Triage, Settings...
│       ├── components/         # PhotoGrid, SwipeCard, Sidebar...
│       └── hooks/              # useApi, useWebSocket, usePrefetch...
│
└── photos.db                   # 共有DB（Python側が管理）
```

### 2.4 フロントエンド技術スタック

| 技術 | 用途 |
|------|------|
| Tauri | デスクトップシェル（軽量、配布10-20MB） |
| React + TypeScript | UI構築 |
| Tailwind CSS | スタイリング |
| Framer Motion | アニメーション（スワイプ、トランジション） |
| TanStack Query | APIデータフェッチ・キャッシュ |

### 2.5 バックエンドAPI設計（api_server.py）

FastAPIで既存コードを薄くラップする。主要エンドポイント：

```
GET  /api/health                    # ヘルスチェック
POST /api/scan/start                # スキャン開始
GET  /api/scan/status               # スキャン進捗（WebSocket推奨）
GET  /api/files?page=&limit=        # ファイル一覧（ページネーション）
GET  /api/files/{id}/thumbnail      # サムネイル取得
GET  /api/files/{id}/preview        # プレビュー画像取得
POST /api/files/{id}/triage         # トリアージ結果登録（keep/discard/skip）
POST /api/files/batch-triage        # バッチトリアージ
GET  /api/duplicates                # 重複グループ一覧
GET  /api/blurry?threshold=         # ぼけ写真一覧
GET  /api/similar?distance=         # 類似グループ一覧
GET  /api/similar/{group}/best      # グループ内ベストショット推薦
POST /api/ai/classify               # AI分類実行
WS   /ws/progress                   # スキャン・解析の進捗ストリーム
```

### 2.6 移行ステップと工数見積もり

| ステップ | 内容 | 工数目安 |
|----------|------|----------|
| Step 1 | Python を API化（FastAPIラッパー追加、PyQt6依存を外す） | ~1週間 |
| Step 2 | Tauriシェル構築（Pythonサイドカー起動、WebSocket接続） | ~2週間 |
| Step 3 | React UIを構築（全画面を新規実装） | ~1-2ヶ月 |

---

## 3. 新機能：トリアージモード

### 3.1 概要

1枚ずつ大きく表示し、キーボードまたはスワイプで「残す / 捨てる / 後で」を高速仕分けする。
**目標ペース: 1枚1〜2秒（1時間で2,000枚処理）。**

### 3.2 UI仕様

- 画面中央に写真を最大サイズで表示
- 上部にプログレスバー（例: 「1,847 / 23,412」）
- 下部に3つのアクションボタン（キーボードショートカット対応）
- 直前10件のUndo機能（スタック管理）

### 3.3 操作マッピング

| 操作 | キーボード | スワイプ | 結果 |
|------|-----------|---------|------|
| 残す | → or D | 右スワイプ | triage_status = 'keep' |
| 捨てる | ← or A | 左スワイプ | triage_status = 'discard' |
| スキップ | ↓ or S | 下スワイプ | triage_status = 'skip' |
| 戻す | Ctrl+Z | — | 直前の操作を取消 |

### 3.4 パフォーマンス要件

- **先読み**: 常に次の3枚をメモリにプリロード
- **送り遅延**: 0ms（先読み済み画像を即座に表示）
- **サムネイルではなく中解像度画像**（1920px長辺）を表示。フル解像度はズーム時のみ

---

## 4. 新機能：AIプリスコアリング

### 4.1 概要

数十万枚を全て人間が見るのは非現実的なので、AIで事前にスコアリングし**「まず見るべき写真」を絞り込む**。

### 4.2 スコアリングの3軸

#### 4.2.1 技術品質スコア（OpenCVベース）

| 指標 | 手法 | 既存/新規 |
|------|------|----------|
| ぼけ | Laplacian variance | 既存 |
| 露出 | ヒストグラム分析（白飛び・黒つぶれ検出） | 新規 |
| ノイズ | 高周波成分比率 | 新規 |

#### 4.2.2 コンテンツ価値判定（CLIPベース）

CLIPプロンプトで以下を判定し、low-value候補を自動マーク：

```python
LOW_VALUE_PROMPTS = [
    "a screenshot of a phone screen",
    "a screenshot of a computer screen",
    "a photo of a receipt",
    "a photo of a document or memo",
    "a blurry or out of focus photo",
    "a photo of text on a whiteboard",
    "a duplicate or near-duplicate photo",
]
```

#### 4.2.3 重複グループ代表選出

類似グループ内で最もシャープな1枚を「代表（best shot）」に選出し、残りを「削除候補」にマーク。既存のpHash + blur_scoreの組み合わせで実装可能。

### 4.3 DB拡張

```sql
ALTER TABLE files ADD COLUMN quality_score REAL;       -- 技術品質（0.0〜1.0）
ALTER TABLE files ADD COLUMN content_type TEXT;         -- 'photo','screenshot','receipt','document','other'
ALTER TABLE files ADD COLUMN triage_status TEXT;         -- 'keep','discard','skip', NULL=未処理
ALTER TABLE files ADD COLUMN is_best_in_group INTEGER;  -- 類似グループの代表=1
```

---

## 5. 新機能：バッチ判定UI

### 5.1 概要

類似グループを一覧表示し、グループごとに「ベストショットを残して残りを捨てる」操作を一括で行う。

### 5.2 UI仕様

- 類似グループをカード形式で一覧表示
- 各カードにグループ内の写真をサムネイルグリッドで表示
- AIが推薦するベストショットにチェックが入った状態で表示
- 「すべて承認」ボタンでAI推薦を一括適用
- 個別にベストショットを変更可能

### 5.3 既存コードとの関係

`modules/similarity_ui.py` の類似グループ検出ロジックはそのまま活用し、UI部分のみReactで再構築。ベストショット推薦ロジックを `core.py` に追加。

---

## 6. DB設計改善：フルスキャン前提

### 6.1 設計方針

**差分更新は信用しない。毎回フルスキャンする。ただし重い処理は自然にスキップされる構造にする。**

### 6.2 フェーズ分離

```
Phase 0: パス収集（数秒）
  └─ os.scandir() でパスとサイズだけ収集
  └─ DBとのdiffを取る（新規/変更/消失）

Phase 1: ハッシュ計算（数分）
  └─ 「新規 or サイズ変更」のファイルだけ8KB先頭MD5を計算
  └─ ハッシュが同じなら既存レコードを再利用

Phase 2: 画像解析（重い）
  └─ ぼけ・pHash・サムネイル・品質スコア
  └─ 「未計算のもの」だけ対象
```

**ポイント**: フルスキャンだが、重い処理は「既に計算済みなら飛ばす」だけ。差分検出ロジックを信用する必要がない。

### 6.3 解析状態の明示的管理

```sql
ALTER TABLE files ADD COLUMN scan_phase TEXT DEFAULT 'registered';
-- 遷移: registered → hashed → analyzed → scored
```

中断しても `WHERE scan_phase = 'registered'` で再開位置がわかる。

### 6.4 DBは使い捨て可能にする

- **サムネイル**: DB（BLOB）ではなくディスクキャッシュ `~/.photosortx/thumbs/{hash}.jpg` に保存
- **解析結果**: ハッシュをキーにしたファイルキャッシュに保存
- **DB全初期化**: `DROP TABLE → CREATE TABLE → 再スキャン` で完全復旧可能。重い計算結果はキャッシュに残る

### 6.5 トランザクション設計

```python
# バッチ単位でatomicに処理
BATCH = 500
for batch in chunked(files, BATCH):
    with db.transaction():
        for f in batch:
            db.upsert(f)
    emit_progress(count)
```

### 6.6 整合性チェッカー

いつでも実行可能な整合性検証機能を提供：

```python
def verify_integrity(db, root_path):
    """ライブラリの整合性を検証し、問題があれば該当レコードだけ再解析する"""
    issues = []
    for row in db.get_all_files():
        path = Path(row['path'])
        if not path.exists():
            issues.append(('missing_file', row))
        if row['scan_phase'] == 'analyzed' and not row['hash_value']:
            issues.append(('hash_missing', row))
        if row['scan_phase'] == 'analyzed' and not thumb_cache_exists(row['hash_value']):
            issues.append(('thumb_missing', row))
    return issues
```

設定画面に「ライブラリを検証」ボタンとして配置。

---

## 7. UI/UXリデザイン

### 7.1 サイドバー再構造化

現在の5セクション → 3セクションに統合：

| 現状 | 新構成 |
|------|--------|
| ステップ0 取込・解析 | **Library** — Home, Gallery, Import |
| ホーム・閲覧 | ↑に統合 |
| 整理の流れ ①〜⑤ | **Clean up** — Duplicates, Blurry, Tiny, Similar, Manual |
| 別フェーズ（スマート整理） | **Smart** — AI organize |
| システム | 右下歯車アイコン → 設定画面に移動 |

- 番号①〜⑤は**廃止**（順序の強制をやめる）
- 各項目にアイコン + 短い説明を付与
- セクション見出しは小さめ大文字 + 淡色
- アクティブ項目は左端アクセントバー + 薄い背景色
- 「DB全初期化」等の危険操作は設定画面の奥に移動

### 7.2 空状態（Empty State / Home）画面

起動直後の画面に以下を配置：

- プロダクトロゴ + 「PhotoSortX」ワードマーク
- 説明文「AIと従来手法を組み合わせた写真整理ツール」
- 2つのCTAカード：「写真を取込む」（プライマリ）/「ライブラリを解析」（セカンダリ）
- 最終解析日時とライブラリ統計情報

### 7.3 デザイントークン（theme管理）

`src/theme.ts`（フロントエンド側）で以下を一元管理：

- カラーパレット（背景、前景、アクセント、警告、成功、danger）
- スペーシング（4px基準: 4, 8, 12, 16, 24, 32）
- タイポグラフィ（見出し3段階 + 本文 + キャプション）
- 角丸・ボーダー
- ダーク/ライトモード切り替え対応

---

## 8. スマホ写真特有の考慮事項

実装時に対応が必要なスマホ写真の特殊ケース：

| 問題 | 対策 |
|------|------|
| 同名ファイル（IMG_0001.jpg が複数フォルダに存在） | パスではなくハッシュでユニーク性を判定 |
| HEIC/HEIF（iPhoneデフォルト） | PillowまたはImageMagickで変換。pillow-heifを使用 |
| Live Photos（.mov + .heic ペア） | ペアを検出し、動画側の扱いポリシーを設定可能にする |
| クラウド同期のゴースト | プレースホルダファイル（0バイトや極小）を検出・除外 |
| 大量のスクショ・レシート | CLIPでcontent_typeを自動判定し、一括処理候補に |

---

## 9. 配布・パッケージング

販売レベルの配布品質を実現するための要件：

| 項目 | 方針 |
|------|------|
| フロントエンド配布 | Tauri（Rust製、配布10-20MB） |
| Python配布 | サイドカーとしてバンドル。PyInstallerではなくNuitka推奨 |
| AIモデル配布 | PyTorch → ONNX Runtime に変換して軽量化。モデルは初回起動時DL |
| コード署名 | EV証明書取得（Windows SmartScreen警告回避） |
| 自動アップデート | Tauri標準のアップデーター機能を利用 |

---

## 10. 実装優先度

効果の大きい順に実装する：

| 優先度 | 項目 | 理由 |
|--------|------|------|
| P0 | Python API化（Step 1） | 全ての前提。ここが終わらないと何も始まらない |
| P0 | トリアージモード | これだけで体験が根本的に変わる |
| P1 | スクショ/レシート自動検出 | 数十万枚の30-50%を一括候補に回せる |
| P1 | Tauriシェル + 基本UI | フロントエンドの基盤 |
| P2 | ベストショット自動選出 | 類似グループ内の代表選出 |
| P2 | サイドバー・Empty State リデザイン | プロダクト品質の仕上げ |
| P3 | 品質スコアリング統合 | 上記が揃ってから統合スコアとして仕上げ |
| P3 | ONNX化・配布最適化 | 販売直前の最終仕上げ |